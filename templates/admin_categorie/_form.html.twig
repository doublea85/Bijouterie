{{ form_start(form, { 'attr' : {'class' : "col-md-6 mx-auto"}}) }}
    {{ form_row(form.nom) }}
    <button class="btn btn-success col-12 mt-3">{{ button_label|default('Ajouter') }}</button>
{{ form_end(form) }}

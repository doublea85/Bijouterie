<form method="post" style="border:none" class="d-inline-block" action="{{ path('app_admin_categorie_delete', {'id': categorie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie.id) }}">
    <button class="btn"><i class="text-danger" data-feather="trash"></i></button>
</form>
